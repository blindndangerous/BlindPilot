__pycache__/
*.py[cod]
.DS_Store
.venv/
.test-cache/
.pytest_cache/
.pytest-tmp/
.test-tmp/
.tmp_*
dist/
dist_new/
build/
*.spec
# BlindPilot.spec is source, not a build artefact: the release workflow
# builds every platform from it and the README links to it, so it must be
# tracked even though PyInstaller's generated .spec files are not.
!BlindPilot.spec
NVIDIA Corporation/
artifacts/
.release-setup/
.venv-test/
.venv-win/
.freebuff/
.ruff_cache/
.claude/settings.local.json
# Scratch config for the visual-audit second instance (docs/visual-audit/README.md)
docs/visual-audit/sandbox/

# Screenshots taken while auditing the look of the app. Viewed at audit time,
# described in words in docs/visual-audit, and not worth 3 MB per clone.
docs/visual-audit/shots/

wxPython>=4.2
markdown-it-py>=3
pyobjc-framework-Cocoa>=10; sys_platform == "darwin"
accessible-output2>=0.17; sys_platform == "win32"
pyte>=0.8
pywinpty>=2.0; sys_platform == "win32"
pexpect>=4.9; sys_platform != "win32"
# Only needed to drive a Hermes running on another machine. The local Hermes
# backend uses a pipe and does not need it, so a missing copy is reported as an
# actionable message rather than stopping the app from starting.
websocket-client>=1.7
